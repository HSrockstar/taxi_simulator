# 演示参数调优分析报告

> 目标：在课程设计题目给定的供需约束下（100 辆出租车、每秒 5–10 个订单、热点区域订单高度集中），
> 找出让"供需失衡检测与动态调度"机制展示效果最好、同时保持真实可信的引擎参数组合。
> 所有结论均由批量仿真扫参工具 [`tools/param_sweep.cpp`](tools/param_sweep.cpp) 驱动真实引擎测得，
> 原始数据见 [`tools/results/`](tools/results/)。
>
> **口径说明**：本报告全部数字基于当前引擎语义——接驾时长按路况 ETA 实时推进、
> 调度触发条件为供需差值**严格超过**失衡阈值（与题目措辞一致）。
> `tools/results/` 中 `recheck_*`、`radiuscheck_*` 为当前口径数据，其余 CSV 为旧口径（接驾钳制
> 2–6 秒、调度触发"达到阈值"）的历史存档，引用时注意区分。

## 1. 约束与可调空间

题目原文定死的内容（**不可调**）：

| 项 | 取值 |
| --- | --- |
| 初始空闲出租车 | 约 100 辆（带评分） |
| 订单生成率 | 每秒 5–10 个 |
| 失衡场景 | 故意让部分区域订单率极高（引擎实现：80% 订单集中在活跃热点 3×3 格，热点每 30 秒在三处坐标间轮换） |

运行时可热更新的参数有 4 个（另有司机数/订单率保持题目值不动）：

| 参数 | 默认值 | 范围 | 语义 |
| --- | --- | --- | --- |
| `matchRadius` 撮合半径 | 3 | 1–20 | 订单撮合时扫描的网格半径 |
| `rebalanceRadius` 调度半径 | 10 | 1–30 | 热点调度搜索供体司机的网格半径 |
| `imbalanceThreshold` 失衡阈值 | 2 | 1–20 | 当 `订单数-空车数` 严格超过该值时触发调度 |
| `orderTimeout` 订单超时 | 10 | 1–60 | 订单等待多少秒后自动取消 |

分析立场：**本系统的考核点是失衡调度机制本身，不追求极限成功率**。取消是题目明确要求的状态流转
（"订单因超时未匹配成功而被乘客取消"），适度的取消量反而体现真实性。

## 2. 分析方法

- **扫参工具**：`tools/param_sweep.cpp` 以 `-DTAXI_TESTING` 编译，直接同步驱动引擎 `testTick()`，
  速度远快于实时，且与线上 exe 共用同一份 `simulator.cpp`。
- **实验单元**：每个参数组合用相同随机种子从头构建 `Simulator`，连续跑 600 模拟秒；
  前 60 秒为运力分布暂态，稳态指标只统计 [60s, 600s] 窗口。
- **指标采集**：逐秒采样订单队列、累计生成/匹配/取消/完成、司机四态（空闲/接客/行程/调度）分布；
  每 0.5 秒解析一次快照日志流，按日志序号去重后精确统计：
  - `[运力调度]` 事件数（调度动画频次）；
  - `[派单成功]` 日志中的预计接驾 ETA、平均路况系数分布；
  - 热力图警报格数（`pending-idle > 失衡阈值`，与前端红框同判据）、路况缓行/拥堵格数。
- **稳健性**：终选组合另用 2 个种子复跑，成功率波动 ≤ 2 个百分点。
- **线上冒烟**：推荐参数通过 `POST /api/params` 写入真实 exe 验证，行为与扫参一致。

## 3. 关键发现

### 3.1 供需格局：结构性紧张是题目的本意

每单平均占用一辆司机约 **18 秒**（接驾时长由路况 ETA 决定，推荐半径下均值约 4 秒、穿过拥堵区的
长单可达十几秒；行程固定 8–20s），因此运力上限 ≈ 司机数 / 18。
需求均值 7.5 单/秒 ≈ **135 个司机当量 > 100 辆**，即题目设定的格局是长期供不应求：

- 成功率存在 ~74% 的理论天花板（= 100/135），现实叠加空间摩擦后 **45–50% 就是"调度机制干得漂亮"的水平**；
- 取消是常态而非缺陷，关键看取消量是否可控、司机是否被充分利用。

### 3.2 失衡调度机制是唯一的决定性杠杆（答辩核心数据）

固定供需、只动调度侧参数，对照组实验（600 模拟秒，3 种子一致）：

| 配置 | 成功率 | 司机空闲率 | 调度动画 |
| --- | --- | --- | --- |
| 调度弱化对照（调度半径 1 + 阈值 5，其余同推荐） | **14.8–16.9%** | **76–79%** | 0 次/分 |
| 机制全开（撮合半径 6 + 调度半径 30 + 阈值 1 + 超时 20） | **48.2–48.9%** | **~30%** | 134–138 次/分 |
| 纯默认参数（r3/d10/t2/w10） | 15.1–16.5% | 77–79% | 33–37 次/分 |

同样的车、同样的订单：**失衡调度机制把成功率提升约 3 倍，把闲置率从 ~78% 压到 ~30%**。
没有调度时，绝大多数司机在离热点很远的地方闲置，热点订单成批超时取消。

### 3.3 各参数的单项效应

**撮合半径（3→12）**：接驾时长由 ETA 决定后，远距离撮合有了真实的时间代价，撮合半径呈现
"温和峰值"而非单调杠杆：同种子下 r3 / r6 / r8 / r10 / r12 的成功率分别为
46.8% / 48.9% / 48.6% / 46.9% / 46.1%，**r6 附近最优**。半径越大接驾 ETA 均值越长
（r6→r12：4.1→7.3 秒），司机被长途接驾占用越久，调度事件也随之变稀（135→84 次/分）。
两点补充：

- **旧口径的"半径悬崖"已消除**：接驾钳制 2–6 秒的时代，撮合半径 ≥10 时热点订单会瞬间吸干周边
  空闲车、调度动画降为 0 次/分；接驾改为按 ETA 推进后，远距离撮合本身耗时，热点不再被瞬间吸干
  （r10/r12 仍有 84–98 次/分调度），半径选择的约束大幅放松；
- **半径越小，调度戏份越足**：r3 时调度事件 149–164 次/分（vs r6 的 134–138），
  且更贴题目"扫描当前网格及相邻网格"的字面语义，热点订单几乎完全依赖调度送车。

**调度半径（20 vs 30）**：30 明显最优。调度行程固定 2 秒、与距离无关，半径拉满没有时效代价，
只扩大供体池：同配比下 d30（48.2–48.9%）比 d20（37.6–39.0%）高约 10 个百分点成功率。
代价仅是青色调度路径最长可横跨约 1/3 地图——视觉上正好演绎"平台从远端调车"。

**失衡阈值（1 vs 2）**：调度触发条件为差值**严格超过**阈值，阈值 1 是最灵敏档（差值 ≥2 即触发）：
成功率 48.2–48.9% vs 阈值 2 的 46.7–48.4%，调度事件 134–138 vs 126–133 次/分。
差距不大，但阈值 1 无任何代价，取最低档即可；前端红框警报与引擎用同一判据，画面和机制完全同步。

**订单超时（15/20/25，配 r6/d30/t1）**：

| 超时 | 成功率 | 稳态队列 | 说明 |
| --- | --- | --- | --- |
| 15s | 44.5–45.5% | ~85 | 换手快，队列数字好看 |
| **20s** | **48.2–48.9%** | **~108** | **平衡点：把"干等到取消"变成"等到车"** |
| 25s | 50.5–52.0% | ~134 | 还能再 +2pp，但队列长期 130+ 像瘫痪，行程完成日志"全程耗时"普遍 30 秒+ |

运力其实够服务大部分订单，订单只是需要等司机到位——超时过短会把这部分订单白白浪费掉
（默认 10 秒下取消来得更早，成功率进一步走低）。但过载本身无法靠延长超时解决（车就那么多），
20 秒是真实感与指标表现的平衡点。

### 3.4 热点轮换的调度响应（可视化证据）

主推参数下观察热点 30 秒轮换瞬间（模拟时间 t=211s）：

```
t=207  队列=132  调度中司机= 5  警报格= 9   ← 旧热点被逐步喂饱
t=211  队列=134  调度中司机= 3  警报格=10   ← 热点切换，新热点警报格涌现
t=212  队列=132  调度中司机=17  警报格=13   ← 调度流向冲向新热点（峰值）
t=215  队列=121  调度中司机= 3  警报格=16   ← 救援到位，警报格逐步回落
t=222  队列= 94  调度中司机= 2  警报格=16   ← 新热点周边运力就位
```

警报格 10→15+"调度中"司机 3→17 再回落——这就是题目要求"直观看到绿格子司机向红格子转移"的完整过程，
演示时每 30 秒固定出现一次。

### 3.5 路况感知 ETA 撮合的可见性

- 主推参数下 **~52%** 的派单日志带显著路况系数（>1.15），备选（撮合半径 3）下高达 **65–68%**，
  可直接指着日志讲"ETA 感知撮合"；
- 接驾时长即 ETA：穿过拥堵区的接驾（日志"平均路况系数 2.50"）实际耗时明显更长，
  派单日志、橙色接客虚线推进速度与全局指标三者互相印证；
- 全图路况层恒定可见：约 129 个缓行格 + 43 个拥堵格（活跃热点 blob 1.5×/2.5× + 两个每 5 秒换位的
  周期事故点）。

## 4. 推荐参数组合

司机数 100、订单率 5–10 为题目固定值，以下仅列 4 个可调参数。成功率/队列等为 3 个种子 × 600 模拟秒的稳态区间。

| | 撮合半径 | 调度半径 | 失衡阈值 | 订单超时 | 成功率 | 稳态队列 | 取消/分 | 调度动画 | 用途 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **① 主推** | 6 | 30 | **1** | 20 | **48–49%** | ~108 | ~230 | 134–138 次/分 | 主演示场 |
| **② 备选** | 3 | 30 | 1 | 15 | 42–43% | ~87 | ~255 | 149–154 次/分 | 机制戏份最足、节奏快 |
| ③ 机制对照 | 5 | 1 | 5 | 20 | ~15% | ~133 | ~377 | 0 | 答辩对比"调度机制贡献" |
| ④ 默认参数 | 3 | 10 | 2 | 10 | 15–16.5% | ~69 | ~377 | 33–37 次/分 | 开场原始状态 |

**各组合的画面与讲解要点：**

- **① 主推**：司机空闲率 ~30%，热力图常态 ~12 个红框警报格，青色调度路径以每分钟约 135 次的节奏
  持续流向热点（常态 ~5 名司机在途调度），橙色接客/紫色行程线交织；日志里 ~52% 派单带显著路况系数
  （如"预计接驾 5.1 秒, 平均路况系数 2.50"），且日志 ETA 就是实际接驾耗时，经得起细看；
  每 30 秒热点轮换带来一次"警报格 10→15、调度流向 3→17"的固定演示点。
- **② 备选**：撮合半径 3 贴题目"相邻网格"字面语义，热点订单几乎全靠调度送车——机制主导地位最清晰；
  路况单占比最高（65–68%），队列 ~87 换手快，ETA 均值 3.2 秒。
- **③ 机制对照**：76–79% 司机闲置、订单成批取消而调度纹丝不动——与 ① 现场切换对比，
  量化"失衡调度把成功率从 ~15% 提到 ~49%、闲置率从 ~78% 压到 ~30%"。
- **④ 默认参数**：成功率 ~16%、调度每分钟仅 35 次几乎不可见，作为"机制未被激活"的叙事起点。

## 5. 演示流程建议

1. **默认参数跑约 1 分钟**：成功率 ~16%、调度稀疏——立住"供需紧张的原始状态"；
2. **在参数面板现场热更新到组合①**（司机数、订单率不动，只调调度侧 4 项）→ 约 30 秒内调度流向
   持续出现、成功率爬升至 ~50%，顺带演示运行时参数热调节功能；
3. **等 30 秒一次的热点轮换**：指出红框迁移、青色路径冲向新热点、队列冲高回落的全过程；
4. **指日志讲路况感知 ETA**：解释"平均路况系数 2.50 表示这条接驾路径穿过拥堵区，接驾也会相应变慢"；
5. **被追问机制有效性时**：切组合③现场对比，或直接引用"~15% vs ~49%、闲置 ~78% vs ~30%"的对照数据；
6. 演示中途切换参数后可点一次"重置"，让指标计数归零、画面更干净。

## 6. 复现方式

```bash
# 编译扫参工具（Windows + MSYS2 UCRT64）
C:/msys64/ucrt64/bin/g++.exe -std=c++17 -O2 -Wall -Wextra -pedantic -DTAXI_TESTING -Isrc \
    tools/param_sweep.cpp src/simulator.cpp src/manual_structures.cpp \
    -o tools/param_sweep.exe -static -static-libgcc -static-libstdc++

# 运行：参数为 [随机种子] [模拟秒数]，输出 sweep_summary.csv / sweep_timeseries.csv
./tools/param_sweep.exe 20260808 600
```

`tools/results/` 保留了本次分析的全部原始数据：

| 文件 | 内容 |
| --- | --- |
| `recheck_*.csv` | **当前口径下 9 个关键组合 × 3 种子的复测数据（本报告数字来源）** |
| `radiuscheck_20260808.csv` | 当前口径下撮合半径 8/10/12 的对照验证（悬崖消除证据） |
| `grid_20260808.csv` | 题目约束下 53 个组合的网格扫描（撮合半径×调度半径×阈值×超时 + 对照组，旧口径） |
| `finetune_*.csv`、`final_*.csv`、`round2~round4_*.csv` | 旧口径下的精调、多种子验证与供需配比扫描 |
| `sweep_summary_20260808.csv` / `sweep_summary_v2_20260808.csv` | 最早期的自由供需扫描（1.0/2.0 版指标） |
| `*_ts_*.csv` | 对应的逐秒时间序列（含热点轮换响应） |

## 7. 已知简化与后续改进方向

- **ETA 与实际接驾时长已统一**：撮合日志中的 ETA 按路况加权估算（基准车速 10 m/s），
  接客状态也按同一 ETA 换算为模拟节拍；短距离至少保留一个节拍，确保状态可见。
- **调度行程固定 2 秒**：供体司机无论来自 50 米还是 300 米外都 2 秒到达，这是引擎的既有简化；
  调度半径拉到 30 依赖该假设。若追求真实感可改为按距离/车速推进（同时建议把调度半径回收至 10–15，
  本报告实测 d20 会损失约 10 个百分点成功率）。
- **同种子完全可复现**：订单生成与撮合同线程同随机序列，演示翻车时可用相同种子重放同一过程
  （命令行 `--seed`）。
