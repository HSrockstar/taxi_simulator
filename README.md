# 智能网约车平台派单与运力调度模拟器

《数据结构与算法设计》课程设计（题目 3）：在 1000×1000 的虚拟城市中模拟网约车平台的实时派单与动态运力调度。系统手工实现了网格空间索引、订单队列、最小堆撮合等核心数据结构，通过 Web 可视化展示供需热力图、派单实时日志与全局指标。

## 功能特性

- **网格化流式撮合**：100 辆带评分的出租车随机分布在 100×100 的网格矩阵中，订单队列按先来先服务处理，每秒批量派单一次。
- **撮合代价评分**：对当前网格及相邻 3 格内的空闲司机计算 `Score = 距离 - 司机评分`，候选压入手工实现的最小堆，堆顶即最优司机。
- **供需失衡动态调度**：统计每个网格的 `订单数 - 空车数`，差值超阈值的格子标记为红色热点区（模拟器内置 3 个热点区域，每 30 秒轮换），自动从相邻 10 格内调度空载司机前往。
- **状态机流转**：司机有 `空闲 / 服务中 / 调度中` 三态，订单有 `等待 / 已匹配 / 已完成 / 已取消`（超时未匹配则取消）四态。
- **运行时参数热调节**：司机数、每秒订单率、撮合半径、调度半径、失衡阈值、订单超时均可通过看板"引擎参数调节"面板或 `POST /api/params` 实时修改，保存后立即生效于下一个模拟秒；修改司机数会即时增减运力。
- **Web 可视化看板**：
  - 网格供需热力图（订单多变红、空车多变绿），可直观看到司机向热点区转移；
  - 派单实时日志（匹配双方、距离、评分、撮合耗时）；
  - 全局指标卡片与趋势图（队列长度、派单成功率、总撮合耗时等）。
  - 后端通过 SSE（Server-Sent Events）把快照实时推送到前端，数据一变即推送，无需轮询。

## 技术栈

| 层 | 技术 |
| --- | --- |
| 后端 | C++17、WinSock2 手写 HTTP 服务器（每连接一线程 + SSE 实时推送）、std::thread 多线程 |
| 前端 | Vue 3 + TypeScript + Vite（构建产物输出到 `web/`，由 C++ 服务静态托管） |
| 构建 | Make + MSYS2 UCRT64 g++、npm |

## 目录结构

```
├── Makefile                  # 构建脚本（前端 + 后端 + 测试）
├── src/
│   ├── main.cpp              # 入口：命令行参数、HTTP 服务启动、浏览器唤起
│   ├── model.h               # 核心常量与数据模型（司机、订单、撮合候选）
│   ├── manual_structures.*   # 手写数据结构：网格索引、订单队列、最小堆、日志环形缓冲
│   ├── simulator.*           # 模拟引擎：订单生产、批量派单、动态调度、状态机
│   └── http_server.*         # WinSock2 HTTP 服务器（API + 静态文件）
├── frontend/                 # Vue 3 + TS 前端源码
│   └── src/components/       # 热力图画布、派单日志、指标卡片、趋势图
├── web/                      # 前端构建产物（由后端直接托管）
├── tests/                    # 单元测试（-DTAXI_TESTING 编译）
└── 作业要求/                 # 课程设计题目原文
```

## 环境要求

- Windows（后端使用了 WinSock2 与 Shell API）
- [MSYS2](https://www.msys2.org/) UCRT64 工具链，Makefile 中默认编译器为 `C:/msys64/ucrt64/bin/g++.exe`（路径不同时请修改 Makefile）
- Node.js + npm（仅构建前端时需要）

## 构建与运行

```bash
# 1. 构建前端并编译后端（产物：taxi_simulator.exe）
make all

# 2. 启动模拟器（默认自动打开浏览器）
./taxi_simulator.exe
```

可执行文件静态链接了 C++ 运行库（`-static`），是免依赖的单文件 exe，双击或拷贝到其他 Windows 机器均可直接运行。

访问 <http://127.0.0.1:8080> 即可看到看板。按 `Ctrl+C` 安全退出。

### 命令行参数

```text
taxi_simulator.exe [--port 8080] [--seed 20260808] [--duration 秒数] [--no-browser]
```

| 参数 | 说明 |
| --- | --- |
| `--port` | HTTP 服务端口，默认 `8080` |
| `--seed` | 随机种子（相同种子可复现模拟过程） |
| `--duration` | 运行指定秒数后自动退出，默认一直运行 |
| `--no-browser` | 启动时不自动打开浏览器 |

### 前端独立开发

```bash
npm --prefix frontend install
npm --prefix frontend run dev     # http://localhost:5173，/api 已代理到 127.0.0.1:8080
```

## HTTP API

后端同时提供 JSON 接口与 `web/` 目录下的静态页面：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/stream` | SSE 实时推送：状态变化时推送完整快照（网格供需、司机/订单状态、指标、参数、近期日志），空闲时发送心跳注释 |
| GET | `/api/snapshot` | 当前快照 JSON（调试用） |
| POST | `/api/control/pause` | 暂停模拟 |
| POST | `/api/control/resume` | 恢复模拟 |
| POST | `/api/control/reset` | 重置模拟（重新随机生成司机与热点，保留当前参数） |
| POST | `/api/params` | 更新模拟参数（JSON body），非法值返回 400，成功返回应用后的参数 |

## 核心设计

### 手工实现的数据结构（`src/manual_structures.*`）

- **GridIndex**：100×100 网格矩阵，坐标到网格 O(1) 定位，每格维护空闲司机双向链表，支持 O(1) 增删与跨格移动。
- **OrderQueue**：链式队列存储未处理订单，互斥锁保护，先来先服务。
- **MinHeap**：动态数组的二叉最小堆，存放撮合候选（司机、分数、距离），堆顶为最优匹配；同分时依次按距离、评分、司机 ID 决胜，保证结果确定性。
- **LogRingBuffer**：容量 200 的环形缓冲区，保存最近日志并按序号增量输出给前端。

### 模拟流程（`src/simulator.*`）

单一调度线程每秒执行一次 tick，所有逻辑在一个状态锁内按固定顺序运行：处理司机状态回退（服务/调度完成回归空闲）→ 生成 5–10 个订单（80% 集中在热点区，热点每 30 秒在三处坐标间轮换）→ 批量派单（出队订单 → 扫描当前格及相邻 3 格空闲司机 → 代价分数压入最小堆 → 取堆顶匹配）→ 统计各格供需差并向热点区调度空载司机。订单等待超过 10 秒未被匹配则自动取消。

由于订单生成与撮合由同一线程、同一随机序列驱动，**相同种子下模拟过程完全确定、可逐 tick 复现**（测试 `testDeterminism` 验证）。

### 可调参数（`src/model.h`）

地图与网格规模为编译期常量；以下模拟参数支持运行时热更新（看板"引擎参数调节"面板或 `POST /api/params`），表中为默认值与合法范围：

```cpp
kMapSize = 1000              // 城市边长（编译期常量）
kGridSide = 100              // 网格边数，每格 10×10（编译期常量）
kMaxDriverCount = 500        // 司机数组容量上限（编译期常量）
```

| 参数 | 默认值 | 范围 | 说明 |
| --- | --- | --- | --- |
| `driverCount` 司机数 | 100 | 10–500 | 增加即时生成新空闲司机，缩减即时移除多余运力 |
| `orderRateMin/Max` 每秒订单率 | 5–10 | 0–50 | 每 tick 生成的订单数量区间 |
| `matchRadius` 撮合半径 | 3 | 1–20 | 撮合扫描的网格半径 |
| `rebalanceRadius` 调度半径 | 10 | 1–30 | 热点调度搜索供体的网格半径 |
| `imbalanceThreshold` 失衡阈值 | 2 | 1–20 | 触发动力调度的 `订单数-空车数` 门槛 |
| `orderTimeout` 订单超时 | 10 | 1–60 | 订单等待多少秒后自动取消 |

## 测试

```bash
make test    # 编译 taxi_tests.exe 并运行
```

覆盖七组用例：网格索引增删与移动、订单队列先进先出、最小堆有序性、撮合与调度算法（含超时取消、热点调度）、同种子确定性复现、运行时参数热更新（司机数/订单率/半径/阈值/超时及越界拒绝）、HTTP 服务器请求处理（含 SSE 流式接口）。
