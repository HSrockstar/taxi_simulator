# 录制视频操作教程与口播稿

适用项目：智能网约车平台派单与运力调度模拟器。这里的“核心测试展示”不使用单元测试终端，而是直接启动 `taxi_simulator.exe`，在看板上展示真实的派单、状态流转和热点运力调度。视频控制在约 3 分钟。

## 录制前准备

1. 使用 OBS 的“显示器采集”录制主屏。不要用窗口采集；切换到浏览器时容易丢失任务栏时间。
2. 在 Windows“任务栏设置 → 任务栏行为”里关闭“自动隐藏任务栏”。录制前确认右下角系统时间清楚可见，视频全程不暂停、不剪辑。
3. 打开 VS Code，提前定位四处代码：`src/model.h` 第 8 行、`src/manual_structures.cpp` 第 62 行、`src/simulator.cpp` 第 133 行与第 497 行、`src/simulator.cpp` 第 544 行。
4. 在 `E:\taxi` 打开 PowerShell，预先输入但先不执行：`./taxi_simulator.exe --seed 20260808`。固定随机种子能让本次演示可复现。
5. 浏览器保持非全屏，缩放为 80% 左右，让城市热力图、参数面板和实时日志尽量同屏，同时保留任务栏时钟。

启动后，在“引擎参数调节”中改为下面这组演示参数，再点击“应用参数”：司机数量 100；订单率下限 5、上限 10；撮合半径 6；调度半径 30；失衡阈值 1；订单超时 20 秒。这是项目中为突出热点调度准备的参数组合。

## 一镜到底操作顺序

| 时间 | 画面操作 | 要讲清楚的点 |
| --- | --- | --- |
| 0:00–0:12 | 开始录制，停在 `model.h` | 系统时间持续可见；城市为 1000×1000 米、100×100 网格；司机和订单都有状态机 |
| 0:12–0:34 | 切到 `manual_structures.cpp` 的 `addDriver`、`removeDriver` | 每格用司机内嵌的双向链表保存空车，增删为 O(1) |
| 0:34–1:05 | 切到 `simulator.cpp` 的 `executeTick`、`findBestDriverLocked` | 每 100 ms 推进；候选按 ETA 减去 0.5×评分进入手写最小堆，堆顶获选 |
| 1:05–1:23 | 定位 `rebalanceLocked` | 供需差严格超过阈值才调度，每个热点格每拍最多调两辆 |
| 1:23–1:34 | 切到 PowerShell，执行 `./taxi_simulator.exe --seed 20260808` | 控制台出现本地地址，浏览器自动打开看板 |
| 1:34–1:58 | 在浏览器显示“引擎运行中”和实时看板；填入并应用演示参数 | 参数会在下一个 100 ms 节拍生效，不需重启模拟器 |
| 1:58–2:40 | 观察热力图、滚动日志和指标；鼠标短暂停在红框热点上 | 红色/红框表示订单积压并超过阈值，青色路径是调度，橙色和紫色虚线分别是接客与行程 |
| 2:40–3:00 | 等待几条日志继续出现，停在看板上结束 | 日志与指标持续刷新，说明前端通过 SSE 接收真实引擎快照 |

## 可直接照读的口播稿

> 现在录制的是智能网约车平台派单与运力调度模拟器。右下角的系统时间全程可见，视频从这里开始不暂停、不剪辑。我先看代码，再直接启动程序看它跑起来。
>
> 城市是 1000 乘 1000 米，被切成 100 乘 100 个格子，每格 10 米。这样派单时不用扫完所有司机，只要从订单所在格向周边查找。引擎 100 毫秒推进一个 tick，一秒十次。
>
> 司机有空闲、接客途中、行程中和调度中四种状态。只有空闲司机放在网格索引里。司机一接单就从链表移除，这样下一笔订单不会重复选到它。订单则从等待变成已匹配，最后完成；等太久会取消。
>
> 这里是手写网格索引。坐标除以 10 得到格子坐标，再压成一维下标访问 10000 个格子。每格存空闲司机的双向链表，指针直接放在 Driver 结构体里。新增用头插，删除只改相邻节点，都是 O(1)。司机接单、结束行程或被调度时，位置都从这里更新。
>
> 再看一个 tick 的顺序：刷新路况，推进在途司机和空闲游走，按秒生成订单，批量撮合，最后检查热点调度。这些步骤在同一把状态锁里执行；随机数也只在调度线程里使用，所以同一个种子能复现同一次模拟过程。
>
> 派单不只是挑最近的车。程序从订单格一圈一圈找候选司机，沿接驾方向采样经过的格子，把畅通、缓行、拥堵的系数换成预计接驾时间，也就是 ETA。分数是 ETA 减去零点五乘司机评分。候选都压进手写最小堆，堆顶获选。近车遇到拥堵时，远一点但路况更好的车也可能先接单。
>
> 这段是热点调度。待处理订单减去空闲车数必须严格大于阈值才触发，等于不算。系统只从周边有富余空车的格子找司机，也按 ETA 和评分进最小堆。每个热点格一个 tick 最多调两辆，车两秒后到达热点，再回到空闲索引里。
>
> 下面启动模拟器。我用固定种子 20260808，控制台会启动 8080 端口的本地服务，浏览器自动打开实时看板。这里显示“引擎运行中”，T 加秒数在走，说明后端正在持续计算。
>
> 我把撮合半径调到 6 格，调度半径调到 30 格，失衡阈值设为 1，订单超时设为 20 秒。点击应用后，下一个 100 毫秒 tick 就会采用新参数，不需要重启。
>
> 现在看热力图。绿色表示空车相对多，红色表示订单积压，红框说明供需差超过阈值。斜线是缓行或拥堵。青色路径是调度车辆，橙色虚线是去接客，紫色虚线是载客行程。鼠标停在格子上，还能看到订单数、空闲车数和路况系数。
>
> 右边的派单、调度、行程开始和完成日志来自 C++ 后端的日志缓冲区，通过 SSE 推到浏览器。等待队列、成功派单和完成行程会随事件变化。这些实时画面，正好对应前面讲的网格索引、最小堆派单和热点调度。

## 录完自检

- 从源码、启动命令到浏览器看板是一个连续画面，任务栏时间全程可见；
- 控制台确实运行了 `taxi_simulator.exe`，浏览器顶部显示“引擎运行中”；
- 看板中至少出现一次红框热点或青色调度路径，并出现持续滚动的实时日志；
- 讲的是实时功能验证，不再说“55 项通过”或展示 `taxi_tests.exe`。
